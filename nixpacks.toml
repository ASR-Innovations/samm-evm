[phases.setup]
nixPkgs = ['nodejs_20', 'rustc', 'cargo', 'gcc']

[phases.install]
cmds = [
  'cd rust-samm && cargo build --release',
  'npm install --production',
]

[start]
cmd = 'node api-server.js'
