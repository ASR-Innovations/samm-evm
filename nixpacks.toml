[phases.setup]
nixPkgs = ['nodejs_20', 'rustc', 'cargo', 'pkg-config', 'openssl', 'gcc']

[phases.install]
cmds = [
  'npm install --production',
]

[phases.build]
cmds = [
  'cd rust-samm && cargo build --release',
  'ls -la rust-samm/target/release/',
  'test -f rust-samm/target/release/samm && echo "✓ Rust binary exists" || (echo "✗ Binary NOT found" && exit 1)',
]

[start]
cmd = 'node api-server.js'
